# Generated / heavy — regenerate at home with `npm install` / `npm run build`
portfolio/node_modules/
portfolio/.next/
portfolio/tmp/
portfolio/out/
portfolio/build/

# Transferable snapshot (don't track the zip of ourselves)
breno-portfolio-snapshot.zip

# Logs / OS
*.log
.DS_Store
Thumbs.db
portfolio/dev.log

# Claude Code local state
.claude/
.claude.json
*.ps1
brand/

# Portfolio case studies — each is its own git repo (see gh repo per case)
portfolio-cases/
