// Shared cadence for the site's sequential reveals (Approach, Principles,
// Process). One source of truth so the three stay in lockstep and the page
// never feels dissonant. The per-item focus-in *duration* lives in CSS as
// --reveal-dur — keep the two in sync if you retune.
export const SEQ_BEAT = 600; // ms between each item opening
export const SEQ_START = 180; // ms before the first item opens
